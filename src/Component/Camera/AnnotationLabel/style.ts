import { StyleSheet } from "react-native";

export const Style = StyleSheet.create({
  annotationLabel: {
    padding: 8,
    margin: 16,
    borderRadius: 50,
    backgroundColor: "#fff",
  },
  annotationLabelText: {
    fontSize: 48,
    color: "#000",
  },
});
